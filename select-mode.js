console.log('A'); const mode = process.env.MODE || 'live'; console.log('B'); let handler; switch(mode) { case 'demo': handler = require('./modes/demo'); break; case 'pay': handler = require('./modes/pay'); break; case 'backtest': handler = require('./modes/backtest'); break; default: handler = require('./modes/live'); break; } console.log('C'); handler.onStart(); module.exports = handler;
