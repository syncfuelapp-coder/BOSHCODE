js console.log('file loaded'); const mode = process.env.MODE || 'live'; let handler; switch(mode) { case 'demo': handler = require('./modes/demo'); break; case 'pay': handler = require('./modes/pay'); break; case 'backtest': handler = require('./modes/backtest'); break; default: handler = require('./modes/live'); break; } console.log('switch done, calling onStart'); handler.onStart(); module.exports = handler; console.log('module exported');
